import React, { Component } from "react";
import { Dropdown, Input, Menu, Col, message } from "antd";

class InputSearch extends Component {
  static getDerivedStateFromProps(nextProps, prevState) {
    // 没错，这是一个static
  }
}
export default InputSearch;
